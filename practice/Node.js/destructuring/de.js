// 객체
const object = {a: 1, b: 2};
// const a = object.a;
// const b = object.b;

const {a, b} = object;
console.log(a, b);

// 배열
const array = [1, 2];

const [one, two] = array;
console.log(one, two);
